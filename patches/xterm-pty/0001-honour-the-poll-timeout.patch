--- a/emscripten-pty.js	2026-09-05 07:57:06.227159575 -0700
+++ b4/emscripten-pty.js	2026-09-05 10:18:38.564690556 -0700
@@ -78,6 +78,21 @@
 
     $PTY_pollTimeout: 0,
 
+    // The timeout the caller of poll(2) actually asked for. emscripten's
+    // own poll implementation calls every stream's poll op with a
+    // hardcoded -1, so the TTY op below cannot see the deadline the
+    // caller computed and would sleep until a keystroke arrived,
+    // ignoring its timers. Recorded on the way in and used instead.
+    $PTY_realPollTimeout: -1,
+
+    // And never sleep longer than this. Asking the TTY to wait throws,
+    // which abandons the scan of the caller's remaining descriptors, so
+    // one that becomes ready while the terminal is quiet is only noticed
+    // once poll returns. Returning early from poll is always allowed, so
+    // a cap costs nothing but keeps such an event from waiting on a
+    // keystroke that may never come. It also bounds the wait below.
+    $PTY_maxPollSleep: 10,
+
     $PTY_askToWaitAgain__deps: ['$PTY_pollTimeout', '$FS'],
     $PTY_askToWaitAgain: (timeout) => {
         PTY_pollTimeout = timeout;
@@ -85,7 +100,7 @@
     },
 
     $PTY_waitForReadableWithCallback__deps: ['$PTY', '$PTY_pollTimeout'],
-    $PTY_waitForReadableWithCallback: (callback) => {
+    $PTY_waitForReadableWithCallback: (callback, boundMs) => {
         if (PTY_pollTimeout === 0) {
             return callback(PTY.readable ? 0 /* ready */ : 2 /* timeout */);
         }
@@ -133,14 +148,26 @@
     $PTY_atomicIndex: 0,
 
     $PTY_waitForReadableWithAtomic__deps: ['$PTY_waitForReadableWithAtomicImpl', '$PTY_atomicIndex', 'malloc'],
-    $PTY_waitForReadableWithAtomic: (callback) => {
+    $PTY_waitForReadableWithAtomic: (callback, boundMs) => {
         if (!PTY_atomicIndex) {
             PTY_atomicIndex = _malloc(4) >> 2;
         }
         HEAP32[PTY_atomicIndex] = -1;
         PTY_waitForReadableWithAtomicImpl(PTY_atomicIndex);
-        Atomics.wait(HEAP32, PTY_atomicIndex, -1);
-        callback(HEAP32[PTY_atomicIndex]);
+        // The thread that answers this is the browser's main thread, and
+        // it may be busy. Waiting on it without a bound parks the caller
+        // for as long as that thread takes, which is how QEMU's main
+        // loop came to stop dead while the page unpacked a closure. A
+        // caller that can retry passes one; waking early is safe for it,
+        // and an answer that arrives afterwards lands on the next wait
+        // as a spurious wake, costing one extra round and nothing more.
+        //
+        // fd_read passes none on purpose: it reads a timeout as end of
+        // input, so an early wake there would close the console.
+        const woke = boundMs === undefined
+            ? Atomics.wait(HEAP32, PTY_atomicIndex, -1)
+            : Atomics.wait(HEAP32, PTY_atomicIndex, -1, boundMs);
+        callback(woke === 'timed-out' ? 2 /* timeout */ : HEAP32[PTY_atomicIndex]);
     },
 #endif
 
@@ -181,7 +208,7 @@
     fd_read__async: true,
 #endif
 
-    $PTY_wrapPoll__deps: ['$PTY_waitForReadable', '$PTY_handleSleep'],
+    $PTY_wrapPoll__deps: ['$PTY_waitForReadable', '$PTY_handleSleep', '$PTY_maxPollSleep'],
     $PTY_wrapPoll: (impl) => PTY_handleSleep((wakeUp) => {
         let result = impl();
         // Did this call return our variant of EAGAIN?
@@ -200,22 +227,28 @@
                         wakeUp(0);
                         break;
                 }
-            });
+            }, PTY_maxPollSleep);
         } else {
             wakeUp(result);
         }
     }),
 
-    __syscall__newselect__deps: ['$xterm_pty_old_newselect', '$PTY_wrapPoll'],
-    __syscall__newselect: (nfds, readfds, writefds, exceptfds, timeout) => (
-        PTY_wrapPoll(() => xterm_pty_old_newselect(nfds, readfds, writefds, exceptfds, timeout))
-    ),
+    __syscall__newselect__deps: ['$xterm_pty_old_newselect', '$PTY_wrapPoll', '$PTY_realPollTimeout'],
+    __syscall__newselect: (nfds, readfds, writefds, exceptfds, timeout) => {
+        // select(2) carries a timeval rather than milliseconds, so read
+        // nothing from it and let the cap above bound the wait.
+        PTY_realPollTimeout = -1;
+        return PTY_wrapPoll(() => xterm_pty_old_newselect(nfds, readfds, writefds, exceptfds, timeout));
+    },
 #if !PROXY_TO_PTHREAD
     __syscall__newselect__async: true,
 #endif
 
-    __syscall_poll__deps: ['$xterm_pty_old_poll', '$PTY_wrapPoll'],
-    __syscall_poll: (fds, nfds, timeout) => PTY_wrapPoll(() => xterm_pty_old_poll(fds, nfds, timeout)),
+    __syscall_poll__deps: ['$xterm_pty_old_poll', '$PTY_wrapPoll', '$PTY_realPollTimeout'],
+    __syscall_poll: (fds, nfds, timeout) => {
+        PTY_realPollTimeout = timeout;
+        return PTY_wrapPoll(() => xterm_pty_old_poll(fds, nfds, timeout));
+    },
 #if !PROXY_TO_PTHREAD
     __syscall_poll__async: true,
 #endif
@@ -223,7 +256,7 @@
 
 // Override default TTY ops to use our PTY.
 // Doing this at compile-time reduces amount of generated unused JS.
-Lib.$TTY__deps.push('$PTY', '$PTY_askToWaitAgain');
+Lib.$TTY__deps.push('$PTY', '$PTY_askToWaitAgain', '$PTY_realPollTimeout', '$PTY_maxPollSleep');
 
 let outputTtyOps = {
     put_char: () => {
@@ -295,7 +328,10 @@
 
     poll: (stream, timeout) => {
         if (!PTY.readable && timeout) {
-            PTY_askToWaitAgain(timeout);
+            const wait = PTY_realPollTimeout < 0 || PTY_realPollTimeout > PTY_maxPollSleep
+                ? PTY_maxPollSleep
+                : PTY_realPollTimeout;
+            PTY_askToWaitAgain(wait);
         }
         return (PTY.readable ? {{{ cDefs.POLLIN }}} : 0) | (PTY.writable ? {{{ cDefs.POLLOUT }}} : 0);
     },

