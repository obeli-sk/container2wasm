--- a/glib/glib-unix.c
+++ b/glib/glib-unix.c
@@ -158,7 +158,10 @@ g_unix_set_fd_nonblocking (gint       fd,
                            gboolean   nonblock,
                            GError   **error)
 {
-#ifdef F_GETFL
+#if defined(__EMSCRIPTEN__)
+  return TRUE;
+#elif defined(F_GETFL)
   glong fcntl_flags;
   fcntl_flags = fcntl (fd, F_GETFL);
 
